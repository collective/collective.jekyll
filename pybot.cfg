[buildout]
parts += supervisor
extends = dev.cfg

[supervisor]
recipe = collective.recipe.supervisor
port = 9001
user = admin
password = admin
programs = 10 instance ${buildout:directory}/bin/instance [console]
